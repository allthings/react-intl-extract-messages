/*
 * Just a placeholder component to check for additionalComponentNames option
 * (see tsconfig and test for extra options)
 */

import * as React from 'react'

export default class Lingo extends React.Component<any> {}

export const LingoFormattedMessage = (props: any): any => undefined
